# Base URLs
api.baseUrl=https://api.example.com
ui.baseUrl=https://www.example.com

# Browser Configuration
browser=chrome
headless=true

# Timeouts
timeout.implicit=10
timeout.explicit=30
timeout.pageLoad=60

# API Keys
api.key=YOUR_API_KEY_HERE
